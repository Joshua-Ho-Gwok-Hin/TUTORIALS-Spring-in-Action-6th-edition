spring:
  datasource:
    url: jdbc:mysql://${MYSQL_HOST:localhost}:3306/taco-cloud
    username: admin
    password: password

  jpa:
    show-sql: true
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: create-drop